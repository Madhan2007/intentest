# Product branding placeholders (doc 35 §4) — Flutter mirror

Mirrors `common/frontend/src/theme/products/` for the mobile client. Same
`product_key` folders, same `product.theme.json` shape, same resolution
order: product default at rest, company override after `/resolve`.

No Dart code currently reads these files — placeholder for the asset drop.
